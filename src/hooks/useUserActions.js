// Import actions del slice
import { useAppDispatch } from './hooks/store'

export const useUserActions = () => {

    const dispatch = useAppDispatch()

    /*const action = () => {
        dispatch(action())
    }

    return { action }*/
}